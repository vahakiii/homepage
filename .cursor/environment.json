{
  "name": "Start Page (static site)",
  "ports": [
    {
      "name": "web",
      "port": 8000
    }
  ],
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0",
      "description": "Serves the zero-dependency start page over HTTP on port 8000. Open http://localhost:8000/ to view index.html."
    }
  ]
}
